#!/usr/bin/env bash

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
COMMON_PVE_SOURCE="${DIR}/../common/pve/source"
COMMON_BASH_SOURCE="${DIR}/../common/bash/source"

echo ${DIR}
echo ${COMMON_PVE_SOURCE}
echo ${COMMON_BAS_SOURCE}
